{% extends 'base.html.twig' %}

{% block title %}Chose your fighter{% endblock %}

{% block body %}
<style>
    *{font: 18px/1.5 sans-serif; margin:0; padding:0;}
    body {display:flex; height:100%;flex-direction:column; align-items:center; background: linear-gradient(white, 85% ,#bf0000); }
    h1 {font-weight:bold; font-size:2em; width:fit-content;}
    h2 {font-size:1.5em; padding:0; margin-bottom:0;width:fit-content;}
    h3 {font-size:1.25em; margin-top:0.5vh; margin-bottom:0.5vh;width:fit-content; }

    .header {display: flex; justify-content:space-between; align-items:center; width:100%; margin:0; padding:0; box-shadow: 0px 0px 5px black;  }
    .header button {margin-left:50%;}
    button{padding:5px; padding-left:10px; padding-right:10px; background-color:#990000; color:white; border:none; border-radius:0.5em}
    button:hover {cursor:pointer; background-color:#690000;}
    
    .example-wrapper { width: 95vw; font: 18px/1.5 sans-serif; display: flex; align-items: center; flex-direction:column; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }

    .fighterCard { display:flex; background:white; padding:5px; justify-content:space-between; align-items:center; border: 1px solid black; border-radius:0.5em;}
    .imgFighters { height: auto; width:35%;}
    .imgSerieFighter { height: auto; width:25%; }
    #gridFighters { display: grid; grid-template-columns: 11vw 11vw 11vw 11vw 11vw 11vw 11vw 11vw; grid-gap: 1vw;}
    
   
    .submit{padding:5px; padding-left:10px; padding-right:10px; background-color:#990000; color:white; border:none; border-radius:0.5em}
    .submit:hover {cursor:pointer; background-color:#690000;}


</style>


<div class="header"> 
    <a href="{{ path('app_character_cp') }}"><button>Retour</button></a>
    <h1>SSBU Pool<h1>

</div>
<div class="example-wrapper">
    <h1>Chose your fighter</h1>

    <div id="gridFighters">
    {% for element in fighters %}
    <a class="fighterCard" href="{{ path("app_character_choice", {id: element.getId()}) }}">
        <img class="imgFighters" src="fighters/250_{{ element.getImagePath() }}"></img>
            <p>{{ element.getName() }}</p>  
        <img class="imgSerieFighter" src="serie/{{ element.getSerie().getImagePath() }}">
    </a>

    {% endfor %}
    {% if is_granted("ROLE_ADMIN") %}
    <a class="fighterCard" href="{{ path("app_create_character") }}"><img class="imgFighters" src="plus.webp">Add a character</img></a>
    {% endif %}
    </div>


</div>
{% endblock %}
